% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zictp_fit.R
\name{zictp.fit}
\alias{zictp.fit}
\title{Maximum Likelihood Estimation for the Zero-Modified CTP Distribution}
\usage{
zictp.fit(
  x,
  a_start = NULL,
  b_start = NULL,
  gama_start = NULL,
  omega_start = NULL,
  method = "BFGS"
)
}
\arguments{
\item{x}{Numeric vector of nonnegative counts.}

\item{a_start}{Optional starting value for parameter a.}

\item{b_start}{Optional starting value for parameter b.}

\item{gama_start}{Optional starting value for parameter gamma.}

\item{omega_start}{Optional starting value for omega (0 < omega < 1).}

\item{method}{Optimization method (default: "BFGS").}
}
\value{
An object of class `"zictpfit"`.
}
\description{
Fits the Zero-Modified Complex TriParametric Pearson (ZM-CTP) distribution
to count data using maximum likelihood estimation.

A logit reparameterization is used for omega to ensure 0 < omega < 1,
and a log reparameterization is used for gamma so that
gama = 2*a + 2 + exp(eta), which guarantees variance existence
throughout optimization.
}
\examples{
\donttest{
set.seed(123)
x <- rzictp(30, a = 1, b = 0.5, gama = 5, omega = 0.3)
fit <- zictp.fit(x)
fit$estimates
}
}
